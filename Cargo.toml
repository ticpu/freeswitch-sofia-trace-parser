[package]
name = "freeswitch-sofia-trace-parser"
version = "0.8.1"
edition = "2021"
license = "LGPL-2.1-or-later"
authors = ["Jérôme Poulin <jeromepoulin@gmail.com>"]
description = "Parser for FreeSWITCH mod_sofia SIP trace dump files"
repository = "https://github.com/ticpu/freeswitch-sofia-trace-parser"
homepage = "https://github.com/ticpu/freeswitch-sofia-trace-parser"
keywords = ["freeswitch", "sip", "parser", "voip", "sofia"]
categories = ["parser-implementations", "network-programming"]
documentation = "https://docs.rs/freeswitch-sofia-trace-parser"
autobins = false
exclude = ["/.github", "/.claude", "/.gitleaks.toml", "/hooks", "/patches", "/scripts", "/CLAUDE.md", "/tests/pidf_torture.rs"]

[lib]
name = "freeswitch_sofia_trace_parser"

[[bin]]
name = "freeswitch-sofia-trace-parser"
path = "src/bin/main.rs"
required-features = ["cli"]

[features]
default = []
cli = ["pcap", "dep:clap", "dep:tracing-subscriber", "dep:regex", "dep:quoted_printable"]
pcap = []
torture-test = ["dep:sip-uri"]
pidf-test = []

[dependencies]
memchr = "2"
tracing = "0.1"
serde_json = "1"
clap = { version = "4", features = ["derive"], optional = true }
tracing-subscriber = { version = "0.3", features = ["env-filter"], optional = true }
regex = { version = "1", optional = true }
quoted_printable = { version = "0.5", optional = true }
sip-uri = { version = "0.2", optional = true }
sip-header = "0.3"

[dev-dependencies]
rayon = "1"
eido = { git = "https://gitlab.cauca.ca/cauca-public/eido", features = ["xml"] }
